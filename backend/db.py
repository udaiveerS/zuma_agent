from sqlalchemy import create_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker
import os
from dotenv import load_dotenv

load_dotenv()

# Get database URL from environment variable - Docker container by default
DATABASE_URL = os.getenv("DATABASE_URL", "postgresql+psycopg://chatuser:chatpass@localhost:5432/chatdb")

engine = create_engine(DATABASE_URL)

SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

Base = declarative_base()

def get_db():
    """Dependency to get database session"""
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

# Table creation is handled by Docker container initialization
# See database/scripts/init/ for schema management
